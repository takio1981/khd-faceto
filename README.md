# ระบบลงเวลาเข้า-ออกงานด้วยใบหน้า (Face Attendance System)

Web app สแกนใบหน้าเพื่อลงเวลาเข้า-ออกงาน พร้อมบันทึกภาพใบหน้า, กำหนดกะ/OT, รายงาน, แดชบอร์ด และระบบสิทธิ์ Admin/User

> **v2026-07-19b** เพิ่มแสดงภาพเต็มเฟรมในประวัติการลงเวลา:
> - **ปุ่ม "ดูภาพ"** เปิด dialog แสดง 2 tab: **"👤 ใบหน้า"** (face crop เหมือนเดิม) + **"🌍 สภาพแวดล้อม"** (ภาพเต็มเฟรมจากกล้อง) — tab สภาพแวดล้อมแสดงเมื่อมี `full_frame_path` เท่านั้น
> - เพิ่ม `full_frame_path` ใน `AttendanceRecord` model
> - `getFullFrameBlob(id)` ใน AttendanceService โหลดภาพเต็มเฟรมจาก `GET /api/attendance/full-frame/:id`
>
> **v2026-07-19a** ยกเลิกระบบตรวจจับโทรศัพท์/อุปกรณ์ทั้งหมด + เพิ่มบันทึกภาพเต็มเฟรมทุก scan:
> - **ลบ Anti-Spoofing ทั้งหมด**: ยกเลิก COCO-SSD phone detection, Phone Bezel Detection, Brightness Grid Detection, chip "🟢🔴 จอ %", toggle ป้องกันการปลอมแปลง
> - **บันทึก 2 ภาพต่อ scan**: (1) ภาพใบหน้า (crop เหมือนเดิม) + (2) **ภาพเต็มเฟรมจากกล้อง** (≤640px) เพื่อให้ admin ดูสภาพแวดล้อม/background ได้โดยตรง
> - **DB Migration 037**: เพิ่มคอลัมน์ `full_frame_path VARCHAR(500)` ใน `attendance_records`
> - **Backend endpoint ใหม่**: `GET /api/attendance/full-frame/:id` (admin only) สำหรับดูภาพเต็มเฟรม
> - COCO-SSD object detector คงไว้เฉพาะ toggle "แยกแยะมนุษย์/สัตว์/วัตถุ" (ปิดอยู่โดย default) — ไม่ auto-load เมื่อเปิดกล้องอีกต่อไป
>
> **v2026-07-18l** เพิ่ม **Screen Brightness + Texture Detection** แก้ปัญหา COCO-SSD ตรวจจับโทรศัพท์ไม่ได้เมื่อถือชิดกล้อง (เห็นแค่จอ ไม่เห็น body):
> - **Signal ① Brightness Grid**: วิเคราะห์เฟรมทั้งหมด 8×6 cells — เซลล์ที่มี mean luminance > 155 + within-cell variance < 700 คือ "screen-like"; หากครอบคลุม ≥35% ของเฟรม = ตรวจพบจอโทรศัพท์ — ทำงานได้แม้ phone body อยู่นอกเฟรม
> - **Signal ② Face Texture (Gradient)**: crop ใบหน้า 24×24 px คำนวณ avg gradient magnitude — ผิวคนจริง > 9 px/cell, ใบหน้า JPEG บนจอ < 9 px/cell; ใช้เป็น secondary signal ยืนยันร่วมกับ brightness
> - **Chip แสดงสถานะ real-time**: 🟢/🔴 จอ XX% แสดงที่มุมล่างเฟรมเสมอเมื่อ Anti-Spoofing เปิด (ใช้ tune threshold ได้)
> - ทั้งสองทำงานเป็น pure Canvas/pixel ops บน canvas ขนาดเล็ก < 1ms ต่อเฟรม ไม่ต้องโหลดโมเดลเพิ่ม
> - ปรับ threshold ผ่าน constants: `SCREEN_CELL_BRIGHT`, `SCREEN_CELL_UNIFORM`, `SCREEN_COVERAGE_RATIO`, `SCREEN_FACE_GRAD_MAX`
>
> **v2026-07-18k** ปรับปรุง Anti-Spoofing เป็น **ตรวจจับโทรศัพท์อย่างเดียว** (COCO-SSD Phone Detection Only):
> - **ยกเลิก Background Blur**: ลบ toggle เบลอพื้นหลังออก + ลบ `#bgCanvas` canvas และ CSS ที่เกี่ยวข้องทั้งหมด
> - **ตรวจจับ 2 ระดับพร้อมกัน**: (1) **Global Block** — COCO-SSD พบโทรศัพท์/laptop/TV ในเฟรม → block ทุก scan 3 วินาที; (2) **Face-on-Screen** — ใบหน้า (centroid) อยู่ภายใน bbox ของอุปกรณ์ → block ทันที
> - **บันทึกแจ้งเตือน Spoofing** ทันทีเมื่อตรวจพบ พร้อมระบุเหตุผล ("ตรวจพบใบหน้าบนจอโทรศัพท์/หน้าจอ" หรือ "ตรวจพบโทรศัพท์มือถือในเฟรม") — แก้ bug เดิมที่ `faceIsOnPhoneScreen()` กรองใบหน้าออกแบบเงียบโดยไม่บันทึก
> - **ลด threshold** จาก 0.2 → 0.1 + เพิ่มคลาส `laptop` และ `tv` (ครอบคลุมการนำหน้าจออื่นมาแสดงภาพ)
> - **ยกเลิก Passive Liveness Phase 2** ทั้งหมด (motion variance + detectScreenFrame pixel analysis) — เหลือแค่ Phase 1 confirm count → scan
>
> **v2026-07-18j** เพิ่ม 2 ฟีเจอร์ใหม่:
> - **Background Blur (เบลอพื้นหลัง)**: เพิ่ม toggle "🌫️ เบลอพื้นหลัง" ในเมนูตั้งค่าอุปกรณ์ — เบลอพื้นหลังรอบใบหน้าแบบ Portrait Mode ด้วย Canvas API (blur 14px ทั้งเฟรม แล้ว cut-out ใบหน้าแบบคมชัดซ้อนทับ) ช่วยซ่อนพื้นหลัง เพิ่มความแม่นยำ Anti-Spoofing และให้ภาพดูเป็นมืออาชีพ | รองรับ Flip H/V ด้วย CSS transform เช่นเดียวกับ video
> - **Checkbox Multi-select ในตาราง Attendance**: (Admin เท่านั้น) เลือกรายการแต่ละแถว หรือ "เลือกทั้งหมด" ด้วย header checkbox | แสดง action bar "เลือกแล้ว N รายการ + ปุ่มลบที่เลือก" | backend เพิ่ม `DELETE /api/attendance` (bulk delete by ids array) | selection reset อัตโนมัติเมื่อเปลี่ยนหน้า/ค้นหาใหม่ | รองรับ card view บนมือถือด้วย
>
> **v2026-07-18i** แก้ false positive Anti-Spoofing: ระบบเคยตรวจจับพื้นหลังมืด (ผ้าม่าน/กำแพง) เป็นกรอบโทรศัพท์ผิดพลาด — เปลี่ยนเงื่อนไขเป็น **ต้องพบ OPPOSITE PAIR** (ซ้าย+ขวา หรือ บน+ล่าง ต้องมืดพร้อมกัน): phone bezel มีกรอบทั้งสองข้าง แต่ background มืดแค่ด้านเดียว | ปรับ threshold: brightness<95, std<35 | Phase 1 early-fail ต้องพบ 3 ครั้ง (จาก 2) เพื่อลด false positive
>
> **v2026-07-18h** ปรับปรุง Anti-Spoofing ให้ทำงานได้แม้ไม่มี COCO-SSD + เพิ่ม toggle เปิด-ปิดในเมนูตั้งค่าอุปกรณ์:
> - **ตรวจกรอบหน้าจอจาก Phase 1** (ไม่ต้องรอช่วง Liveness Window): ตรวจจับขอบหน้าจอโทรศัพท์/แท็บเล็ต (dark uniform bezels) ทุก 2 เฟรม ตั้งแต่เริ่มพบใบหน้า — เมื่อพบ 2 ครั้ง ระบบบันทึก Spoofing Alert ทันที ไม่รอ 3 วินาที
> - **ปรับ threshold ให้จับได้ง่ายขึ้น**: brightness<80 → brightness<130, std<25 → std<45, ต้อง 2 strip → ต้อง 1 strip, ลด gap จาก 8px → 3px (รองรับ bezel บาง) พร้อม fallback gap=0
> - **faceIsOnPhoneScreen()**: เมื่อ COCO-SSD พร้อม ตรวจ geometric overlap ระหว่าง face bbox กับ phone bbox (>25% = phone screen face → บล็อกทันที ไม่ต้องรอนับ)
> - **สวิตช์ Anti-Spoofing** ในเมนูตั้งค่าอุปกรณ์ (เปิดเป็นค่าเริ่มต้น) พร้อมแสดงสถานะ COCO-SSD แบบ real-time
> - ทั้งหมดนี้ทำงานโดยไม่ต้องรอ COCO-SSD โหลด — การตรวจกรอบหน้าจอ (pixel analysis) ทำงานได้ทันที
>
> **v2026-07-18g** ปรับปรุงระบบตรวจจับโทรศัพท์มือถือ (Anti-Phone-Spoofing) ให้เชื่อถือได้มากขึ้น + อนุญาตออกงานโดยไม่ต้องเข้างานก่อน:
> - **Global Phone Block**: ใช้ COCO-SSD ตรวจจับ 'cell phone'/'remote' ใน object-detector loop (1วินาที/รอบ) → เมื่อพบโทรศัพท์ตั้ง block 4 วินาที ทุก scan ถูกหยุดทันที
> - **อัปเกรด COCO-SSD model** จาก `lite_mobilenet_v2` → `mobilenet_v2` ให้แม่นยำในการตรวจจับโทรศัพท์มากขึ้น
> - **ออกงานโดยไม่ต้องเข้างานก่อน**: หากอยู่ในช่วงเวลาออกงานแต่ยังไม่มีบันทึกเข้างานวันนี้ ระบบอนุญาตให้สแกนออกงานได้
>
> **v2026-07-18f** แก้ไขการตรวจจับโทรศัพท์มือถือ (Phone Spoofing): (1) Object Detector เริ่มทำงานเร็วขึ้น 6s → 1s (2) ย้ายการตรวจจับโทรศัพท์ขึ้นมาก่อน Phase 1/2 (3) ลด confidence threshold 0.35 → 0.25 (4) ปรับ reset timer
>
> **v2026-07-18e** เพิ่มการจัดการข้อมูลหน้า Security Alerts: **ลบรายเดียว** (ปุ่ม 🗑 ต่อแถว) + **เลือกหลายรายการแล้วลบพร้อมกัน** (checkbox + ปุ่ม "ลบที่เลือก N รายการ") — ลบทั้งข้อมูลในฐานข้อมูลและไฟล์ภาพใบหน้า
>
> **v2026-07-18d** ปรับ Anti-Spoofing เป็น **Passive Liveness** ไม่ต้องกะพริบตา — ใช้ 3 วิธีผสม: (1) **Micro-motion tracking** ติดตาม centroid ใบหน้า 3 วินาที หากไม่มีการขยับตามธรรมชาติ (variance < 0.5 px²) = ภาพนิ่ง (2) **COCO-SSD Phone Detection** ตรวจจับโทรศัพท์มือถือในเฟรมแบบ real-time (3) **Screen-frame detection** วิเคราะห์ pixel รอบใบหน้าหาขอบจอมืด | Object Detector เปิดอัตโนมัติตลอดเวลา | หน้า Security Alerts ใช้ **date picker ปฏิทิน** แทน input วันที่ธรรมดา
>
> **v2026-07-18c** เพิ่ม **ระบบ Liveness Detection (Anti-Spoofing)**: ป้องกันการนำภาพนิ่งจากโทรศัพท์มาสแกนแทนกัน — ระบบบังคับให้ **กะพริบตา 1 ครั้งภายใน 4 วินาที** หลังยืนยันตัวตน หากไม่กะพริบ จะบันทึกภาพผู้ที่ถือภาพหน้ากล้องลงตาราง `spoofing_alerts` | admin ดูรายการ+ภาพใบหน้าได้ที่เมนู **"แจ้งเตือนปลอมแปลงใบหน้า"**
>
> **v2026-07-18b** เพิ่มความแม่นยำการจำใบหน้า: backend คำนวณ **mean descriptor** ต่อพนักงานอัตโนมัติ (centroid ของทุก pose ที่ลงทะเบียน) — ช่วยจับมุมกลางที่ไม่ได้ถ่ายตรงๆ | หน้าพนักงานแสดง badge **สีเหลือง ⚠ เมื่อมีภาพน้อยกว่า 5 มุม** เพื่อแจ้งให้ลงทะเบียนใหม่ครบ 5 มุม (หน้าตรง/ซ้าย/ขวา/ก้ม/เงย)
>
> **v2026-07-18** แก้ปัญหากะวันหยุด (holiday_shift_id) ไม่ทำงานในวันเสาร์-อาทิตย์ — ระบบจะใช้กะวันหยุดอัตโนมัติทั้งใน **วันเสาร์-อาทิตย์** และ **วันหยุดประกาศ** (holidays table)
>
> **v2026-07-15** แก้ปัญหาข้อความ "บันทึกไปแล้ว/รออยู่ในช่วงคูลดาวน์" แสดงผิดสำหรับคนที่ยังไม่มีบันทึกเข้างานช่วงเวลาออกงาน — ระบบจะแสดง "ยังไม่มีบันทึกเข้างาน กรุณาสแกนเข้างานก่อน" แทน | เพิ่ม **กะวันหยุด** (holiday_shift_id) ให้กำหนดกะเฉพาะสำหรับวันหยุดประกาศต่อพนักงาน ระบบสแกนจะใช้กะวันหยุดอัตโนมัติเมื่อวันนั้นอยู่ในตารางวันหยุด

เทคโนโลยี: **Node.js + TypeScript + Express**, **MariaDB**, **Angular 19 + Angular Material** (frontend), **face-api.js** (สแกนใบหน้าในเบราว์เซอร์), **Chart.js**, **Leaflet**, **Docker Compose**

---

## ความสามารถหลัก

| หมวด | รายละเอียด |
|------|-----------|
| สแกนใบหน้า | ใช้ webcam (Logitech) ลงเวลาเข้า-ออก + บันทึกภาพใบหน้าทุกครั้ง **รองรับหลายคนพร้อมกัน** |
| เลือกกล้อง | เลือกอุปกรณ์กล้องที่เชื่อมต่อได้ (จดจำค่าที่เลือกไว้) ทั้งหน้าสแกนและหน้าลงทะเบียน; บนมือถือ/แท็บเล็ตมีปุ่ม **สลับกล้องหน้า-หลัง** โดยเฉพาะ |
| กะการทำงาน | กำหนดช่วงเข้างาน / สายได้ถึง / ออกงาน / OT ได้เอง |
| จัดการพนักงาน | เพิ่ม-แก้-ลบ (soft delete), **ลงทะเบียนใบหน้า 3 ภาพ** (มีตัวเลข 1·2·3 + เครื่องหมายถูกสีเขียว), สร้างบัญชีเข้าระบบ |
| รายงาน | รายวัน / รายเดือน / รายปี — ส่งออก **Excel (.xlsx)** และ **PDF** |
| แดชบอร์ด | สรุปสถานะวันนี้ (โดนัท), แนวโน้ม 7 วัน (แท่ง), เจาะลึกรายบุคคล (เส้น) |
| ค้นหา/กรอง | ตามช่วงวันที่, พนักงาน, สถานะ (ตรงเวลา/สาย/ขาด/OT) |
| สิทธิ์ | **admin** = จัดการทั้งหมด, **user** = ดูเฉพาะข้อมูลตนเอง |

---

## วิธีติดตั้งและรัน (Docker — แนะนำ)

ต้องมี **Docker Desktop** ติดตั้งและเปิดอยู่

```bash
# 1) สร้างไฟล์ .env จากตัวอย่าง (แก้รหัสผ่าน/JWT_SECRET ให้ปลอดภัย)
cp .env.example .env

# 2) ตั้ง SERVER_LAN_IP ใน .env เป็น IP ของเครื่องนี้ในระบบเน็ตเวิร์ค/Wi-Fi
#    หา IP: Windows รัน  ipconfig  แล้วดูที่ "IPv4 Address" ของ Wi-Fi/Ethernet
#    (ใส่ได้หลาย IP คั่นด้วย comma ถ้าเครื่องมีทั้ง Wi-Fi และ Ethernet)

# 3) สร้างและรัน
docker compose up --build -d

# 4) เปิดเบราว์เซอร์บนเครื่องนี้เอง
#    http://localhost:3000/khd-faceto/
#    (เปิดแค่ http://localhost:3000 เฉยๆ จะ redirect ไปที่ /khd-faceto/ ให้อัตโนมัติ)
```

> **สำคัญ:** การสแกนใบหน้าใช้กล้องผ่าน `getUserMedia` ซึ่งเบราว์เซอร์อนุญาตเฉพาะบน **localhost** หรือ **HTTPS** เท่านั้น

### เข้าใช้งานจากมือถือ/แท็บเล็ต/เครื่องอื่นในเครือข่ายเดียวกัน

ระบบเปิดให้บริการ **HTTPS** คู่กับ HTTP เพื่อให้กล้องใช้งานได้จากอุปกรณ์อื่นด้วย (ไม่ใช่แค่เครื่อง server เอง):

1. ตรวจสอบว่าตั้ง `SERVER_LAN_IP` ใน `.env` ตรงกับ IP จริงของเครื่อง server แล้ว (ดูขั้นตอนด้านบน)
2. จากมือถือ/แท็บเล็ต/PC อื่นที่ต่อ **Wi-Fi/เครือข่ายเดียวกัน** เปิด:
   ```
   https://<IP ของเครื่อง server>:3443/khd-faceto/
   ```
   เช่น `https://192.168.1.50:3443/khd-faceto/`
3. เบราว์เซอร์จะเตือนว่า "ไม่ปลอดภัย/ใบรับรองไม่น่าเชื่อถือ" เพราะเป็น **self-signed certificate** (ไม่ได้ออกโดย CA สาธารณะ) — กด **"Advanced" → "Proceed anyway"** (หรือ "ไปยังเว็บไซต์นี้") **ครั้งเดียว** ต่ออุปกรณ์ หลังจากนั้นกล้องจะใช้งานได้ปกติบนอุปกรณ์นั้น
4. ถ้าเชื่อมต่อไม่ได้เลย (ไม่ใช่แค่เตือนใบรับรอง) ให้ตรวจสอบ Windows Firewall ว่าอนุญาต inbound TCP port `3443` (และ `3000`) — เปิด PowerShell แบบ Administrator แล้วรัน:
   ```powershell
   New-NetFirewallRule -DisplayName "KHD-FaceTo HTTPS" -Direction Inbound -Protocol TCP -LocalPort 3443 -Action Allow
   ```

> ถ้า IP ของเครื่อง server เปลี่ยน (เปลี่ยน Wi-Fi, DHCP จ่าย IP ใหม่ ฯลฯ) ให้แก้ `SERVER_LAN_IP` ใน `.env` แล้ว `docker compose up -d --build app` ใหม่ — ระบบจะออกใบรับรองใหม่ให้ตรงกับ IP ปัจจุบันโดยอัตโนมัติ

### บัญชีเริ่มต้น
```
Username: admin
Password: admin1234
```
**โปรดเปลี่ยนรหัสผ่านหลังเข้าใช้ครั้งแรก** (สร้างผู้ใช้ใหม่และปิด/เปลี่ยนของเดิม)

---

## ขั้นตอนการใช้งานครั้งแรก

1. **เข้าสู่ระบบ** ด้วย admin
2. ไปที่ **กะการทำงาน** → ตรวจ/แก้กะเริ่มต้น "กะปกติ" ให้ตรงกับเวลาทำงานจริง
3. ไปที่ **พนักงาน** → เพิ่มพนักงาน (กรอกรหัส, ชื่อ, เลือกกะ)
4. กดปุ่ม **ใบหน้า** ของพนักงาน → เลือกกล้อง → เริ่มกล้อง → **ถ่ายภาพ 3 ครั้ง** (ช่อง 1·2·3 จะขึ้นเครื่องหมายถูกสีเขียวทีละภาพ) → กดบันทึก
5. ไปที่ **ลงเวลา (สแกน)** → เลือกกล้อง → กดเริ่มสแกน → หันหน้าเข้ากล้อง (ยืนพร้อมกันหลายคนได้) → ระบบลงเวลาให้อัตโนมัติ
6. ดูผลที่ **ประวัติการลงเวลา**, **แดชบอร์ด**, และ **รายงาน**

---

## ตรรกะการจำแนกการสแกน

ระบบตัดสินว่าเป็น เข้างาน/ออกงาน/OT จากเวลาปัจจุบันเทียบกับกะของพนักงาน:

- อยู่ในช่วง `[checkin_start, late_cutoff]` และยังไม่เข้างาน → **เข้างาน**
  (ก่อน `checkin_end` = ตรงเวลา, หลังจากนั้น = สาย)
- อยู่ในช่วง `[checkout_start, checkout_end]` และเข้างานแล้ว → **ออกงาน**
- อยู่ในช่วง `[ot_start, ot_end]` → **OT**
- สแกนซ้ำภายใน `FACE_COOLDOWN_MINUTES` นาที → ระบบข้ามให้ (กันบันทึกซ้ำ)

> เวลาทั้งหมดอิงตาม `TZ` ใน `.env` (ค่าเริ่มต้น `Asia/Bangkok`) — ต้องตั้งให้ตรงกับเขตเวลาของออฟฟิศ

---

## ตั้งค่า (.env)

| ตัวแปร | ความหมาย |
|--------|----------|
| `TZ` | เขตเวลา (ต้องตรงกับสถานที่จริง) |
| `JWT_SECRET` | กุญแจเซ็น token — **ต้องเปลี่ยนเป็นค่าสุ่มยาว** |
| `FACE_MATCH_THRESHOLD` | ระยะ Euclidean สูงสุดที่ถือว่าตรงกัน (น้อย=เข้มงวด, ค่าแนะนำ 0.5) |
| `FACE_COOLDOWN_MINUTES` | กันสแกนซ้ำกี่นาที |
| `FACE_IMAGE_DIR` | โฟลเดอร์เก็บภาพใบหน้า (map เป็น Docker volume) |
| `COMPANY_NAME` | ชื่อหน่วยงาน แสดงบน navbar + หัวรายงาน (ปัจจุบัน: สำนักงานสาธารณสุขจังหวัดนครราชสีมา) |
| `APP_NAME` | ชื่อระบบ แสดงบน navbar + หน้า login (ปัจจุบัน: ระบบลงเวลา KHD-FaceTo) |
| `HTTPS_PORT` | พอร์ต HTTPS สำหรับอุปกรณ์อื่นในเครือข่าย (ค่าเริ่มต้น 3443) |
| `SERVER_LAN_IP` | IP ของเครื่อง server ในเครือข่าย (ใส่ได้หลาย IP คั่นด้วย comma) — ใช้ออกใบรับรอง HTTPS ให้ตรงกับ IP นี้ |
| `TZ` | เขตเวลา (Asia/Bangkok) |
| `PUBLIC_BASE_URL` | (ไม่บังคับ) โดเมนสาธารณะจริงของระบบรวม `/khd-faceto` เช่น `https://attendance.example.go.th/khd-faceto` — ใส่เพื่อให้ LINE Messaging API ส่งภาพที่สแกนได้แนบไปกับการแจ้งเตือนได้ (ไม่ใส่ก็ใช้งานได้ปกติ เพียงแต่ LINE จะได้แค่ข้อความ) |

---

## โครงสร้างโปรเจกต์

```
khd-faceto/
├── docker-compose.yml        # app + mariadb + volumes
├── .env / .env.example
├── backend/                  # Node + TypeScript API
│   ├── src/
│   │   ├── routes/           # auth, employees, attendance, shifts, reports, dashboard
│   │   ├── services/         # faceCache (จับคู่ใบหน้า), shift (จำแนกสแกน), report (xlsx/pdf)
│   │   ├── middleware/       # JWT auth, error handler
│   │   └── index.ts
│   ├── db/migrations/        # SQL รันอัตโนมัติตอน MariaDB บูตครั้งแรก
│   └── assets/fonts/         # ฟอนต์ไทย Sarabun สำหรับ PDF
└── frontend-ng/               # Angular 19 + Angular Material SPA
    ├── src/app/
    │   ├── core/              # services (API), guards, interceptors, models
    │   ├── shared/             # app-shell (navbar/sidenav), responsive-table, dialogs
    │   └── features/          # login, dashboard, checkin, employees, attendance, reports, shifts, settings
    └── public/lib, public/models/  # face-api.min.js + โมเดล (โหลดแบบ on-demand เฉพาะหน้าที่ใช้กล้อง)
```

> Express เสิร์ฟ Angular build ที่ `frontend-ng/dist/frontend-ng/browser` (คอมไพล์ผ่าน multi-stage `backend/Dockerfile` ระหว่าง `docker compose up --build`) — ไม่ต้องสั่ง build แยก

---

## คำสั่งที่ใช้บ่อย

```bash
docker compose logs -f app      # ดู log แอป
docker compose down             # หยุด (เก็บข้อมูล)
docker compose down -v          # หยุด + ลบข้อมูลทั้งหมด (เริ่มใหม่หมด)
docker compose up --build -d    # build ใหม่หลังแก้โค้ด
```

### เชื่อมต่อฐานข้อมูลโดยตรง
```bash
docker compose exec mariadb mariadb -ukhdapp -p khd_attendance
# ตัวอย่าง: ดูการลงเวลาล่าสุด
# SELECT e.full_name, ar.scan_time, ar.scan_type, ar.status
#   FROM attendance_records ar JOIN employees e ON e.id=ar.employee_id
#  ORDER BY ar.scan_time DESC LIMIT 10;
```

---

## ข้อมูลทดสอบ (Mock Data) — สำหรับดูฟีเจอร์ Dashboard/รายงาน

มีสคริปต์สร้างพนักงานสมมุติ 10 คน + ประวัติการลงเวลาตั้งแต่ต้นปีถึงวันนี้ (พนักงานรหัสขึ้นต้น `DEMO`)

```bash
# สร้าง/รีเฟรชข้อมูลทดสอบ (รันซ้ำได้ — ลบของเดิมที่ขึ้นต้น DEMO ก่อน)
docker compose cp ./backend/scripts/seed-mock.js app:/app/seed-mock.js
docker compose exec app node /app/seed-mock.js
docker compose restart app
```

ลบข้อมูลทดสอบทั้งหมด (เหลือเฉพาะข้อมูลจริง):
```bash
docker compose exec mariadb mariadb -ukhdapp -p"$DB_PASSWORD" khd_attendance \
  -e "DELETE ar FROM attendance_records ar JOIN employees e ON e.id=ar.employee_id WHERE e.employee_code LIKE 'DEMO%'; \
      DELETE fd FROM face_descriptors fd JOIN employees e ON e.id=fd.employee_id WHERE e.employee_code LIKE 'DEMO%'; \
      DELETE FROM employees WHERE employee_code LIKE 'DEMO%';"
```

> **หมายเหตุ:** ใบหน้าของพนักงาน DEMO เป็นค่าสุ่ม (ไม่ตรงกับใบหน้าจริงของใคร) ใช้สำหรับดูรายงาน/แดชบอร์ดเท่านั้น

---

## หมายเหตุด้านความปลอดภัย

- ออกแบบสำหรับใช้งานภายในองค์กร (kiosk/LAN) — มี HTTPS ในตัว (self-signed) สำหรับ LAN/Wi-Fi เท่านั้น; หากเปิดสู่อินเทอร์เน็ตจริงควรใช้ reverse proxy + ใบรับรองจาก CA สาธารณะแทน
- ภาพใบหน้าเข้าถึงได้เฉพาะผ่าน API ที่ตรวจสิทธิ์ (`/api/attendance/image/:id`) ไม่ใช่ static สาธารณะ
- รหัสผ่านเก็บแบบ bcrypt, ทุก API ป้องกันด้วย JWT + rate limit

---

## ประวัติการอัปเดต

- **2026-06-26** — ย้าย frontend ทั้งหมดจาก HTML/CSS/JS ธรรมดาไปเป็น **Angular 19 + Angular Material** (responsive: ตารางพับเป็นการ์ดบนมือถือ/แท็บเล็ตแทนการเลื่อนแนวนอน), ปรับธีมสี/โลโก้ให้ดูทันสมัยขึ้น (เหรียญตรา ✚ ไล่เฉดเขียว-teal, เงา, การ์ดโค้งมน), เพิ่มปุ่มแสดง/ซ่อนรหัสผ่านที่หน้า login, ลบหน้า "รายการลงเวลาล่าสุด" ที่ซ้ำกันในหน้าสแกนเพื่อขยายพื้นที่กล้อง, push ขึ้น GitHub ครั้งแรก
- **2026-06-26** — แก้ error `Cannot read properties of undefined (reading 'enumerateDevices'/'getUserMedia')` ที่หน้าสแกน/ลงทะเบียนใบหน้า เมื่อเปิดผ่าน URL ที่ไม่ใช่ localhost/HTTPS (เบราว์เซอร์ไม่เปิด `navigator.mediaDevices` ให้) — ตอนนี้ระบบจะแจ้งข้อความภาษาไทยที่ชัดเจนแทน ("กรุณาเปิดผ่าน http://localhost:3000 บนเครื่องที่ต่อกล้องอยู่")
- **2026-06-26** — เพิ่ม **HTTPS listener (port 3443, self-signed cert)** คู่กับ HTTP เดิม เพื่อให้มือถือ/แท็บเล็ต/เครื่องอื่นในเครือข่าย Wi-Fi เดียวกันใช้กล้องสแกนใบหน้าได้ (ก่อนหน้านี้ใช้กล้องได้เฉพาะที่ `http://localhost:3000` บนเครื่อง server เท่านั้น เพราะ browser บล็อก getUserMedia บน plain-HTTP ข้ามเครื่อง) — ตั้งค่า IP ของเครื่อง server ผ่าน `SERVER_LAN_IP` ใน `.env`, ใบรับรองออกอัตโนมัติให้ตรงกับ IP ที่ตั้งไว้ (ดูหัวข้อ "เข้าใช้งานจากมือถือ/แท็บเล็ต...")
- **2026-06-26** — เพิ่มปุ่ม **สลับกล้องหน้า-หลัง** ที่หน้าสแกน (checkin) และหน้าลงทะเบียนใบหน้าพนักงาน สำหรับใช้งานบนมือถือ/แท็บเล็ต (ใช้ `facingMode` constraint แทนการเลือก deviceId ตรงๆ ซึ่งเชื่อถือได้กว่าบนมือถือที่ label กล้องมักไม่ชัดเจน) — ปุ่มจะแสดงอัตโนมัติเฉพาะอุปกรณ์ที่รองรับการสัมผัสหน้าจอ (touch device)
- **2026-06-26** — เพิ่มปุ่ม **"กลับหน้าหลัก"** ที่หน้าสแกน (checkin) — พาไปหน้าแดชบอร์ดถ้าล็อกอินอยู่ (เช่น admin เปิดมาจาก navbar) หรือหน้า login ถ้ายังไม่ได้ล็อกอิน (เช่น kiosk สาธารณะ)
- **2026-06-26** — โหมดเต็มจอของหน้าสแกนแสดง **ข้อมูลคนล่าสุดที่สแกนแล้ว** (รูป/ชื่อ/สถานะ/เวลา) เป็นกล่องเล็กมุมขวาบนทับกล้อง (ก่อนหน้านี้ตอนเต็มจอจะมองไม่เห็นข้อมูลนี้เลย เพราะ panel รายการสแกนอยู่นอกพื้นที่เต็มจอ) — **แก้เพิ่ม**: กล่องนี้ไม่เคยแสดงจริงเพราะ CSS selector ผิด (`:host-context(.cam-wrap:fullscreen)` ใช้ผิดบริบท เพราะ `.cam-wrap` เป็นลูกของ component ไม่ใช่ ancestor) เปลี่ยนเป็น `.cam-wrap:fullscreen ...` ตรงๆ แทน ตอนนี้แสดงผลถูกต้องแล้ว
- **2026-06-27** — เพิ่มโครงสร้างองค์กร (กลุ่มงาน/แผนก + หัวหน้าแต่ละระดับ) และระบบคำขอแก้ไขเวลา/อุทธรณ์ขาด-สาย แบบ 2 ขั้นตอน (หัวหน้าแผนก/กลุ่มงานอนุมัติ → admin ยืนยัน ซึ่งจะปรับปรุงประวัติลงเวลาให้จริง) — ดูรายละเอียดใน ROADMAP.md หัวข้อ C4
- **2026-06-28** — เพิ่มตารางตำแหน่งมาตรฐาน (สายงานสาธารณสุข) และระดับข้าราชการ (ปฏิบัติการ/ชำนาญงาน ฯลฯ) เชื่อมโยงกับพนักงานและตำแหน่งแบบ cascading dropdown พร้อม validate ความสอดคล้องตำแหน่ง-ระดับ; seed โครงสร้างกลุ่มงาน/แผนกตั้งต้นตามมาตรฐาน สสจ. ทั่วไป (ยังไม่ใช่ข้อมูลจริงของจังหวัดนครราชสีมา เพราะดึงจากเว็บ korathealth.com ไม่ได้ — เมนูเว็บเข้ารหัส URL ไว้ แก้ไขให้ตรงจริงได้ในหน้าตั้งค่า)
- **2026-06-28** — เปลี่ยนโลโก้ทั้งระบบจากเหรียญตรา "✚" เป็นไอคอนสแกนใบหน้าที่ออกแบบใหม่ (กรอบมุมแบบกล้อง + รูปหน้าคน) ใช้ที่หน้า login, navbar, และ favicon
- **2026-06-28** — กล่องข้อมูลล่าสุดในโหมดเต็มจอของหน้าสแกน เปลี่ยนจากแสดงแค่ "คนล่าสุด 1 คน" เป็น **รายการทุกคนที่สแกนแล้ววันนี้ทั้งหมด เรียงคนล่าสุดไว้บนสุด** เลื่อนดูได้ (เหมือนกับ panel รายการสแกนด้านข้างตอนไม่เต็มจอ)
- **2026-06-28** — หน้าสแกน (checkin): ปรับสัดส่วนคอลัมน์ภาพกล้อง/ฟีดข้อมูลให้กล้องใหญ่ขึ้นชัดขึ้น (จาก 1.7:1 เป็น 2.6:1 พร้อมจำกัดความกว้างฟีดสูงสุด), เพิ่มปุ่มสวิทช์แสดง/ซ่อนจุดบนใบหน้า, ลดดีเลย์ของ loop ตรวจจับใบหน้าจาก 400ms เป็น 150ms ให้ตอบสนองเร็วขึ้น, และเพิ่ม exponential smoothing ให้กรอบ/จุดบนใบหน้าไม่กระตุกขณะติดตามหน้า
- **2026-06-28** — หน้าสแกน (checkin) อีกรอบ: ช่องภาพ/ข้อมูลการสแกนล่าสุดเปลี่ยนเป็นความกว้างคงที่ (280px) ชิดด้านขวาสุดแบบ sticky แทนการกินพื้นที่ตามสัดส่วน grid เพื่อให้ช่องกล้องใหญ่ที่สุดเท่าที่จะทำได้; เพิ่มแถบเลื่อนปรับ **ความเร็วในการตรวจจับใบหน้า** (80-600ms) และ **ความสมูทในการติดตามใบหน้า** (0.1-0.9) ในเมนู "ตั้งค่าอุปกรณ์" พร้อมคำแนะนำการปรับใต้แถบเลื่อนแต่ละอัน ค่าที่ปรับจะจำไว้ในเครื่อง (localStorage)
- **2026-06-28** — แก้บั๊กกรอบ/ชื่อที่วาดทับใบหน้าไม่ตรงตำแหน่งเมื่อมีคนมากกว่า 1 คนในกล้องพร้อมกัน — สาเหตุคือระบบจับคู่กรอบที่วาดแบบ smooth กับใบหน้าโดยอิงตำแหน่ง index ในอาเรย์ ซึ่ง face-api.js ไม่รับประกันว่าลำดับจะเหมือนกันทุกเฟรม ทำให้กรอบของคนหนึ่งไหลไปเกาะกับอีกคนเมื่อมีหลายหน้า เปลี่ยนเป็นจับคู่ตามตำแหน่งศูนย์กลางที่ใกล้ที่สุดจากเฟรมก่อนหน้าแทน ทำให้แต่ละคนมีกรอบของตัวเองตามตัวถูกต้องแม้มีหลายหน้าพร้อมกัน
- **2026-06-28** — กล่อง "✓ บันทึกสำเร็จ" เปลี่ยนจากแถบเต็มความกว้างที่ค้างอยู่ถาวร (ดันให้กล้องเลื่อนลง) เป็นกล่อง toast เด้งขึ้นที่มุมซ้ายบนของจอกล้อง แล้วหายไปอัตโนมัติหลัง 3.5 วินาที (ทำงานทั้งโหมดปกติและเต็มจอ); และปรับกล่อง "ภาพและข้อมูลการสแกนล่าสุด" ในโหมดเต็มจอให้สูงเต็มจอภาพพอดี (เดิมมีขอบเว้น 14px บน-ล่าง)
- **2026-06-28** — แก้ปัญหาหน้าสแกนค้าง (ไม่ขึ้นกรอบใบหน้า ค้างที่ "กำลังค้นหาใบหน้า..." ตลอด ต้อง refresh หน้าจึงกลับมาทำงาน) — สาเหตุคือสตรีมกล้องหยุดนิ่ง/ตายเงียบๆ (เช่น ระบบปฏิบัติการพักกล้อง/ไดรเวอร์สะดุด) โดยไม่มี error ใดๆให้ดักจับ ตัวตรวจจับใบหน้าจึงทำงานกับภาพนิ่งภาพเดิมไปเรื่อยๆ ไม่เจอใบหน้าใหม่ตลอดไป เพิ่ม "watchdog" ตรวจสุขภาพกล้องทุก 4 วินาที (เทียบ `video.currentTime` ว่าขยับหรือไม่) และฟัง event ที่กล้องหยุดทำงาน — ถ้าพบว่าสตรีมค้างจะเชื่อมต่อกล้องใหม่ให้อัตโนมัติพร้อมแจ้งสถานะ โดยไม่ต้อง refresh หน้าอีกต่อไป
- **2026-06-28** — แก้ปัญหาหน้าสแกนค้างรอบที่ 2 (ยังเกิดซ้ำหลังแก้ครั้งแรก) — พบสาเหตุจริงคือ**การประมวลผลตรวจจับใบหน้าของ face-api.js ค้างเอง** (ไม่ใช่ตัวกล้อง/วิดีโอค้างแบบที่แก้ไปก่อนหน้า) ซึ่งมักเกิดจาก TensorFlow.js/WebGL หลุด context หรือเบราว์เซอร์หรี่ทรัพยากรให้แท็บที่ถูกพักไว้ — ทำให้ promise ของการตรวจจับไม่เคย resolve/reject เลย ตัวแปร `busy` จึงค้างเป็น true ตลอดไป และ loop หยุดเรียกตรวจจับซ้ำอีกเงียบๆ แก้ด้วยการจับเวลาทุกครั้งที่ตรวจจับ (timeout 6 วินาที/ครั้ง) ถ้าค้างติดกัน 3 ครั้ง (~18 วินาที) ระบบจะแจ้งสถานะ "ระบบตรวจจับใบหน้าค้าง — กำลังรีเฟรชหน้าอัตโนมัติ..." แล้ว refresh หน้าให้เองโดยอัตโนมัติ (เทียบเท่าการกด refresh มือ ซึ่งเป็นวิธีเดียวที่เคลียร์ปัญหานี้ได้จริง)
- **2026-06-28** — เพิ่มแถบเลื่อนแบบ 2 ปุ่ม **"ระยะห่างในการตรวจจับใบหน้า"** ในเมนู "ตั้งค่าอุปกรณ์" — กำหนดได้ว่าใบหน้าต้องมีขนาดอย่างน้อย/มากที่สุดกี่ % ของความสูงภาพจากกล้องจึงจะถูกนับว่าตรวจพบ (ขนาด % สูง = ใกล้กล้อง, ขนาด % ต่ำ = ไกลกล้อง) ค่าต่ำสุดคือระยะไกลสุดที่ยังตรวจจับได้ ค่าสูงสุดคือระยะใกล้สุดที่ยังตรวจจับได้ (กันใบหน้า/วัตถุจ่อเลนส์) ค่าเริ่มต้น 8%-90% ค่าที่ปรับจำไว้ในเครื่อง
- **2026-06-28** — เพิ่ม**เสียง "ติ๊ง" แจ้งบันทึกสำเร็จ** ทุกครั้งที่สแกนใบหน้าแล้วบันทึกลงเวลาเข้า-ออกได้จริง (สังเคราะห์เสียงด้วย Web Audio ไม่ต้องใช้ไฟล์เสียง ใช้งานได้แม้ไม่มีอินเทอร์เน็ต) พร้อมปุ่มเปิด/ปิดเสียงในแถบควบคุมข้างกล้อง (ค่าเริ่มต้นเปิดเสียง จำการตั้งค่าไว้ในเครื่อง)
- **2026-06-28** — กล่อง "ภาพและข้อมูลการสแกนล่าสุด" ในหน้าสแกน เปลี่ยนจากเก็บข้อมูลแบบ local (เก็บไว้ในเบราว์เซอร์เครื่องนั้นๆ ไม่อัปเดตตามการแก้ไข) เป็น**ดึงจากประวัติการสแกนจริงในฐานข้อมูล** ผ่าน endpoint สาธารณะใหม่ `GET /api/attendance/recent` (จำกัดเฉพาะวันนี้ + จุดสแกนของเครื่องนั้น + 20 รายการล่าสุด) ระบบ poll ข้อมูลใหม่ทุก 8 วินาที และ refresh ทันทีหลังบันทึกสแกนสำเร็จ — ทำให้ถ้า admin แก้ไขหรือลบประวัติการสแกนจากหน้า "ประวัติการลงเวลา" ข้อมูลในกล่องนี้จะอัปเดตตามภายในไม่กี่วินาทีโดยไม่ต้อง refresh หน้า (ทดสอบแล้ว: แก้สถานะผ่าน API แล้วตรวจสอบว่า endpoint นี้คืนค่าใหม่ถูกต้อง)
- เปลี่ยนไอคอนใน favicon.svg ให้ใช้เส้น/รูปทรงเดียวกับโลโก้ที่ใช้ในแถบเมนูและหน้า login เป๊ะๆ (ก่อนหน้านี้ออกแบบไว้คนละชุด ใกล้เคียงกันแต่ไม่ตรงกัน)
- แก้ปัญหาไอคอนที่แท็บเบราว์เซอร์ยังเป็นโลโก้ Angular เริ่มต้น (สามเหลี่ยมสีม่วง-ชมพู) ทั้งที่ฝั่งเซิร์ฟเวอร์ตั้งค่า favicon ใหม่ถูกต้องแล้ว (ตรวจสอบแล้วว่า HTML ที่เซิร์ฟเวอร์ส่งมาอ้างอิงไฟล์ใหม่ถูกต้อง) — สาเหตุคือ Chrome แคชไอคอนแยกจากแคชหน้าเว็บปกติ ผูกกับ URL เดิมเหนียวมาก ไม่รีเฟรชแม้ hard reload แก้ด้วยการเพิ่ม query string `?v=2` ต่อท้าย URL ของ favicon ใน index.html เพื่อบีบให้เบราว์เซอร์มองว่าเป็นไฟล์คนละตัวและดึงใหม่
- แปลงไฟล์ `public/favicon.svg` เป็นไฟล์ `.ico` จริง (multi-size 16/32/48px) แทนที่ `public/favicon.ico` เดิมซึ่งเป็นไอคอน Angular เริ่มต้นที่ยังไม่เคยถูกแก้ไขมาตั้งแต่สร้างโปรเจกต์ — บางเบราว์เซอร์/บางสถานการณ์ดึง `/favicon.ico` ตรงๆโดยไม่สนใจ `<link rel="icon">` ที่ประกาศไว้ ทำให้ยังเห็นโลโก้เดิมแม้ตั้งค่า SVG ใหม่ถูกต้องแล้ว (แปลงโดยใช้ sharp + png-to-ico เป็นเครื่องมือชั่วคราว ไม่ได้เพิ่มเป็น dependency ถาวรของโปรเจกต์) พร้อมขยับ cache-busting เป็น `?v=3`
- เพิ่มหน้า **"ประวัติการแจ้งเตือน"** (เมนูใหม่ ทุก role เห็น) ให้พนักงานแต่ละคนดูประวัติการแจ้งเตือนของตัวเอง (มาสาย/ขาดงาน/ลงเวลาสำเร็จ) พร้อมสถานะอ่าน/ยังไม่อ่าน, ตัวกรอง (ประเภท/สถานะอ่าน/ช่วงวันที่), ทำเครื่องหมายอ่านแล้ว-ยังไม่อ่านทีละรายการหรือทั้งหมด, ลบรายการ — เดิม log การแจ้งเตือน (`notification_inbox`) เป็นของ admin อย่างเดียวและไม่ทราบว่าเป็นของใคร เพิ่มคอลัมน์ `employee_id`+`is_read` ให้แล้ว (ดูรายละเอียดใน ROADMAP.md)
- เพิ่มหน้า **"จัดการผู้ใช้งาน"** (admin-only) สำหรับเพิ่ม/แก้ไข/ลบบัญชีเข้าระบบ ผูก/แก้ไขการผูกกับพนักงาน, รีเซ็ตรหัสผ่าน, ปลดล็อกบัญชีที่ถูกล็อกจากกรอกรหัสผิดหลายครั้ง พร้อมตัวกรอง (สิทธิ์/ค้นหา/ยังไม่ผูกพนักงาน/ถูกล็อกอยู่) — เดิมสร้างบัญชีได้ทางเดียวคือ checkbox ตอนเพิ่มพนักงานใหม่เท่านั้น แก้ไข/รีเซ็ตรหัสผ่านทีหลังไม่ได้เลย (ดูรายละเอียดใน ROADMAP.md)
- หน้า "พนักงาน" (`/employees`) เพิ่มคอลัมน์ **"บัญชีผู้ใช้"** แสดงชื่อผู้ใช้+สิทธิ์ที่ผูกกับพนักงานคนนั้น (มองย้อนกลับจากหน้าจัดการผู้ใช้งาน) ทำให้ดูได้ทั้งสองทางว่าใครผูกกับใคร
- ปรับการตรวจจับใบหน้าให้ทนทานขึ้นกับกรณี**ก้มหน้า/เงยหน้า/หันซ้าย-ขวา/ใส่แมสปิดจมูก-ปาก**: เพิ่มแถบเลื่อน "ความไวในการตรวจจับใบหน้า" ในตั้งค่าอุปกรณ์ (ลดค่าเริ่มต้นจาก 0.5 เป็น 0.35) และขยายหน้าลงทะเบียนใบหน้าจาก 3 เป็น **5 ภาพแบบระบุมุมชัดเจน** (ตรง/ซ้าย/ขวา/ก้ม/เงย) เพื่อให้ข้อมูลใบหน้าที่เก็บไว้ครอบคลุมมุมที่สแกนจริงเจอบ่อยๆ — ยังไม่ได้ทดสอบกับกล้อง/คนจริง เนื่องจากเป็นพารามิเตอร์ AI ที่ทำงานในเบราว์เซอร์
- ตรวจสอบภาพลงทะเบียนจริงของพนักงาน 001 พบว่าภาพ "หันซ้าย/ขวา" เดิมหันจนเกือบเป็นภาพโปรไฟล์และตามองออกจากกล้อง (มุมแรงเกินไป) — แก้คำแนะนำในหน้าลงทะเบียนให้ชัดเจนว่าต้องหันแค่ 15-20 องศา และตา/ใบหน้ายังต้องมองเข้ากล้องอยู่เสมอ พร้อมกล่องคำเตือนสีเหลืองเด่นชัด แนะนำให้ลงทะเบียนใบหน้าใหม่ให้พนักงาน 001 ตามแนวทางที่แก้ไขแล้ว
- เพิ่มตัวเลือก **"แจ้งผู้บังคับบัญชา"** ในตารางเหตุการณ์แจ้งเตือน (มาสาย/ขาดงาน/ลงเวลาสำเร็จ) ในหน้าตั้งค่า — เปิดแล้วระบบจะส่งแจ้งเตือนไปยังหัวหน้างานของพนักงานคนนั้น (ตามที่ตั้งไว้ในช่อง "ผู้บังคับบัญชา") ผ่าน Email/LINE/Telegram และขึ้นในหน้าประวัติการแจ้งเตือนของหัวหน้างานด้วย (ต้องเปิด "รับการแจ้งเตือน" ของหัวหน้างานเองด้วยเช่นกัน)
- เปลี่ยนการเข้าใช้งานระบบให้ต้องต่อท้าย **`/khd-faceto/`** หลัง port เสมอ (เช่น `http://localhost:3000/khd-faceto/`) เพื่อให้รันร่วมกับแอปอื่นบนเครื่อง/พอร์ตเดียวกันได้โดยไม่ชนกันที่ path ราก — เปิดแค่ `http://localhost:3000` เฉยๆ จะ redirect ไปที่ path ใหม่ให้อัตโนมัติ
- **2026-06-29** — แจ้งเตือนสแกนใบหน้าทุกประเภทตอนนี้แนบ**ภาพที่สแกนได้**ไปด้วย (ผ่าน Email เป็นไฟล์แนบ, Telegram ผ่าน `sendPhoto`, และแสดงในแอปเองที่หน้า "ประวัติการแจ้งเตือนของฉัน") พร้อมรายละเอียดเพิ่ม (เวลาที่สแกนแบบเต็ม + ชื่อจุดสแกน); แก้ไปพร้อมกันคือ race condition เดิมที่การบันทึกภาพแยกเป็นคนละจังหวะกับการส่งแจ้งเตือน (ย้ายมาบันทึกก่อนส่งแจ้งเตือนในจุดเดียวกันแล้ว)
- เพิ่มเหตุการณ์แจ้งเตือนใหม่ **"พบใบหน้าที่ไม่รู้จัก"** (`unknown_face`) — แจ้งเฉพาะผู้ดูแลระบบเท่านั้น (ไม่มีพนักงาน/หัวหน้างานให้แจ้ง) ตั้งค่าได้ในส่วน "เหตุการณ์สำหรับผู้ดูแลระบบเท่านั้น" ของหน้าตั้งค่า → แจ้งเตือน — ระบบหน่วงการแจ้งเตือนซ้ำในจุดสแกนเดียวกันไว้ ~5 นาที เพื่อไม่ให้แจ้งเตือนถี่เกินไปจากการสแกนต่อเนื่องของกล้อง
- **ย่อขนาดไฟล์ภาพที่บันทึกทุกภาพ** ให้ไม่เกิน 480px ต่อด้าน + บีบอัด JPEG quality 72 เสมอ (ใช้ `sharp` ฝั่งเซิร์ฟเวอร์ ไม่ว่าอุปกรณ์สแกนจะส่งภาพความละเอียดสูงแค่ไหนมาก็ตาม) — ลดพื้นที่จัดเก็บและขนาดไฟล์แนบในอีเมล/Telegram ลงมาก โดยยังคมชัดพอสำหรับตรวจสอบใบหน้าด้วยตา
- **เพิ่มการส่งภาพผ่าน LINE Messaging API ได้แล้ว** — ใช้วิธีออก URL ภาพชั่วคราวที่มี token สุ่มเดาไม่ได้ฝังเวลาหมดอายุไว้ในตัว (~15 นาที, เซ็นด้วย HMAC ไม่ต้องมีตารางฐานข้อมูลเพิ่ม) แทนการเปิดเป็น URL สาธารณะถาวร เพื่อให้ยังรักษาการคุ้มครองข้อมูลใบหน้าตาม PDPA ไว้ในระดับหนึ่ง — **ต้องตั้งค่า `PUBLIC_BASE_URL`** ใน `.env` เป็นโดเมนสาธารณะจริงที่มี HTTPS ใบรับรองถูกต้อง (รวม `/khd-faceto` ต่อท้าย เช่น `https://attendance.example.go.th/khd-faceto`) เพราะ LINE server ดึงภาพเองโดยตรง เข้าถึง IP ในเครือข่าย LAN หรือใบรับรองที่ออกเองสำหรับกล้องไม่ได้ — ถ้าไม่ได้ตั้งค่าไว้ ระบบจะส่งแจ้งเตือน LINE แบบข้อความอย่างเดียวเหมือนเดิมโดยไม่ error
- **ย้าย COCO-SSD ไปรันใน Web Worker** แยก realm ออกจาก face-api.js เพื่อแก้ปัญหา TF.js backend `cpu` ถูก register ซ้ำกัน (ก่อนหน้านี้มี ~100 warning "The kernel X for backend cpu is already registered" ทุกครั้งที่ initialise); เพิ่ม `DETECTION_TIMEOUT_MS` จาก 6s เป็น 15s ให้เพียงพอสำหรับ WebGL shader compilation รอบแรกของ GPU
- **ตัวเลือกเวลาแบบดิจิทัล 24 ชั่วโมง** ในหน้าคำขอแก้ไขเวลา (แทน `<input type="time">` เดิม) — component `app-time-picker` พร้อมปุ่มลูกศรขึ้น/ลง, scroll wheel, และพิมพ์ตรงได้; ผสานกับ Angular reactive form ผ่าน `ControlValueAccessor`
- **Tab ตั้งค่าเขตเวลา (Timezone)** ใหม่ในหน้าตั้งค่าระบบ — เลือกได้ระหว่าง "อัตโนมัติ (ตามเบราว์เซอร์)" หรือ "กำหนดเอง" พร้อม dropdown ค้นหา IANA timezone ได้ทั้งหมด (~400 รายการ); บันทึกค่าใน `localStorage` ผ่าน `TimezoneService`
