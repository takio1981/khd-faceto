# แผนพัฒนาต่อเนื่อง — khd-faceto

> ไฟล์นี้คือแนวทางพัฒนาระบบต่อไป **ให้ตรวจสอบไฟล์นี้ทุกครั้งก่อน/หลังอัปเดตระบบ** ว่ามีอะไรทำไปแล้ว เหลืออะไรบ้าง — อัปเดตสถานะ (`- [ ]` → `- [x]`) และเพิ่มวันที่/commit ทุกครั้งที่ทำงานเสร็จแต่ละข้อ
>
> สร้างจากผลวิเคราะห์ทั้งระบบเมื่อ 2026-06-27 (ความปลอดภัยไซเบอร์, PDPA, พ.ร.บ.คอมพิวเตอร์, ฟีเจอร์งานบุคคล, UX ผู้บริหาร)
>
> **กฎการทำงาน**: ทำทีละข้อ เรียงตามลำดับความสำคัญด้านล่าง — เสนอแผนของแต่ละข้อให้อนุมัติก่อนเริ่มเขียนโค้ดทุกครั้ง ห้ามข้ามไปทำข้อถัดไปจนกว่าข้อปัจจุบันจะ build ผ่าน ทดสอบแล้ว และ commit/push เรียบร้อย

---

## กลุ่ม A: กระทบกฎหมาย/ความถูกต้องของข้อมูล (ทำก่อน)

- [x] **A1. ปฏิทินวันหยุด/เสาร์-อาทิตย์ในการคำนวณ "ขาดงาน"** — เสร็จ 2026-06-27
  - เพิ่มตาราง `holidays` (migration `015_create_holidays.sql`)
  - เพิ่ม `holidays.service.ts` + `holidays.routes.ts` (`GET/POST/DELETE /api/holidays`)
  - หน้า Settings → ส่วน "ปฏิทินวันหยุด" (เพิ่ม/ลบ, กรองตามปี)
  - แก้ `dashboard.routes.ts` (ไม่นับ absent บนวันหยุด/เสาร์-อาทิตย์ + ส่ง `isNonWorkday` ให้ frontend แสดงแบนเนอร์) และ `notification.service.ts` (ข้ามการแจ้งเตือนขาดงานทั้งวันถ้าเป็นวันหยุด/เสาร์-อาทิตย์)
  - เพิ่ม `ictIsWeekend`/`isWeekendDateKey` ใน `ict.ts`
  - ทดสอบแล้ว: วันหยุดที่ตั้งไว้และวันเสาร์-อาทิตย์ → `isNonWorkday: true, absent: 0`; วันทำงานปกติ → คำนวณตามจริง

- [ ] **A2. PDPA — ระบบขอความยินยอม (consent) ก่อนเก็บข้อมูลใบหน้า**
  ข้อมูลใบหน้า = ข้อมูลอ่อนไหวตาม พ.ร.บ.คุ้มครองข้อมูลส่วนบุคคล มาตรา 26 ต้องมีความยินยอมชัดแจ้ง
  - เพิ่ม consent checkbox + บันทึกวันเวลา/เวอร์ชันที่ยินยอม ก่อนขั้นตอนลงทะเบียนใบหน้า (employees → face-enroll-dialog)
  - ตาราง `consent_log` (employee_id, consented_at, consent_version, withdrawn_at)
  - หน้า Privacy Notice (ประกาศความเป็นส่วนตัว) ภาษาไทย เข้าถึงได้จากหน้า login/enrollment

- [ ] **A3. Audit log การกระทำของ admin**
  ไม่มี log ว่าใครแก้/ลบ attendance, เปลี่ยน setting, ดูภาพใบหน้าใคร — เสี่ยงทั้ง PDPA และ พ.ร.บ.คอมพิวเตอร์ (สอบสวนเหตุไม่ได้)
  - ตาราง `audit_log` (user_id, action, target_table, target_id, before/after JSON, ip, created_at)
  - เขียน log ในทุก mutation route ของ admin (employee/shift/settings/attendance edit-delete, ดูภาพใบหน้า)
  - หน้า Settings → ดู audit log (admin only, filter ตามวันที่/ผู้ใช้/ประเภทการกระทำ)

- [ ] **A4. นโยบายเก็บ/ลบข้อมูล (data retention) + Backup**
  - กำหนดระยะเวลาเก็บภาพใบหน้า/ประวัติสแกน (เช่น ตามระเบียบราชการ) + job ลบอัตโนมัติเมื่อพ้นกำหนด
  - สิทธิ "ขอลบข้อมูล" สำหรับพนักงานที่ลาออก (ผูกกับ PDPA มาตรา 30-36)
  - Script/cron backup mysqldump + sync โฟลเดอร์ภาพใบหน้าไปเก็บที่อื่นเป็นระยะ พร้อมเอกสาร RTO/RPO

- [ ] **A5. ช่องทางใช้สิทธิเจ้าของข้อมูล (PDPA Data Subject Rights)**
  หน้าให้พนักงาน/แอดมินดำเนินการ ขอเข้าถึง/แก้ไข/ลบ/เพิกถอนความยินยอม/ขอย้ายข้อมูลของตน

---

## กลุ่ม B: เสริมความปลอดภัย/การใช้งาน

- [ ] **B1. เข้ารหัสข้อมูลใบหน้าที่ rest** (descriptor ใน DB + ไฟล์ภาพ) ด้วย AES-256
- [ ] **B2. ย้าย JWT จาก localStorage → httpOnly secure cookie** (ลดความเสี่ยง XSS) — ต้องปรับ CORS/CSRF คู่กัน
- [ ] **B3. Rate limit เพิ่มเติมที่หน้า login** (per-IP, หรือ CAPTCHA หลังผิดติดต่อกัน) แยกจาก lockout ที่มีอยู่แล้ว
- [ ] **B4. แดชบอร์ดสรุปสำหรับผู้บริหาร** — เทียบสถิติรายแผนก, % มาสาย/ขาดงานทั้งหน่วยงาน, KPI สรุป, โหมดพิมพ์หน้าเดียว
- [ ] **B5. ตรวจสอบความถูกต้องของบริการแจ้งเตือนข้ามประเทศ** (LINE/Telegram/Gmail) ว่าต้องระบุใน privacy notice เรื่องส่งข้อมูลไปต่างประเทศหรือไม่ (PDPA มาตรา 28-29)
- [ ] **B6. Accessibility**: ตรวจ/เพิ่ม aria-label ให้ครบ, แก้ `lang="en"` → `lang="th"`, โหมด contrast สูง/ปรับขนาดตัวอักษร
- [ ] **B7. ลด validity ของ self-signed cert จาก 10 ปีเป็น 1 ปี + เพิ่มเอกสารขั้นตอน trust cert ให้ผู้ใช้งานทั่วไป

---

## กลุ่ม C: ฟีเจอร์งานบุคคล (HR) เต็มรูปแบบ — ทำเมื่อขยายระบบ

- [ ] **C1. ระบบลา** — ประเภทการลา, ยื่นคำขอ, วันลาคงเหลือ, ปฏิทินการลา
- [ ] **C2. สายบังคับบัญชา/โครงสร้างองค์กร** — ผู้บังคับบัญชา, สายงาน, ลำดับขั้นอนุมัติ
- [ ] **C3. ประเภทบุคลากร** (ข้าราชการ/พนักงานราชการ/ลูกจ้าง) พร้อมกฎเวลาทำงาน/สิทธิที่ต่างกัน
- [ ] **C4. คำขอแก้ไขเวลาจากพนักงาน** → ผู้บังคับบัญชาอนุมัติ → admin ยืนยัน (แทนการแก้ตรงโดย admin คนเดียว)
- [ ] **C5. แบบฟอร์มราชการมาตรฐาน** (เช่น แบบสรุปเวลาปฏิบัติงานราชการ) เป็น PDF template ทางการ
- [ ] **C6. รายงานหลายแผนก/หลายสาขา** (รวม scan_locations เข้ากับโครงสร้างองค์กร)

---

## ทำไปแล้ว (อ้างอิงจากการ migrate Angular และปรับปรุงก่อนหน้านี้)

- [x] Migrate frontend → Angular 19 + Angular Material (responsive, ตารางพับเป็นการ์ดบนมือถือ) — 2026-06-26
- [x] ปรับธีมสี/โลโก้ให้ทันสมัยขึ้น — 2026-06-26
- [x] ปุ่มแสดง/ซ่อนรหัสผ่านหน้า login — 2026-06-26
- [x] HTTPS listener (self-signed cert) สำหรับใช้กล้องจากมือถือ/แท็บเล็ตในเครือข่ายเดียวกัน — 2026-06-26
- [x] ปุ่มสลับกล้องหน้า-หลังบนมือถือ/แท็บเล็ต — 2026-06-26
- [x] ปุ่มกลับหน้าหลักที่หน้าสแกน — 2026-06-26
- [x] แสดงข้อมูลคนล่าสุดที่สแกนแล้วในโหมดเต็มจอ (พร้อมแก้ CSS selector ที่ผิด) — 2026-06-26
- [x] แก้ปัญหา error กล้องเมื่อเปิดผ่าน URL ที่ไม่ปลอดภัย — 2026-06-26
- [x] วิเคราะห์ระบบทั้งหมด (ความปลอดภัย, PDPA, พ.ร.บ.คอมพิวเตอร์, ฟีเจอร์ HR, UX ผู้บริหาร) — 2026-06-27
- [x] A1: ปฏิทินวันหยุด + ไม่นับขาดงานวันเสาร์-อาทิตย์/วันหยุด — 2026-06-27

---

## วิธีใช้ไฟล์นี้

1. ก่อนเริ่มงานใหม่ทุกครั้ง: เปิดไฟล์นี้ดูว่ามีข้อไหนค้างอยู่/เกี่ยวข้องกับงานที่จะทำ
2. เลือกข้อถัดไปตามลำดับ A → B → C เสนอแผนสั้นๆ ให้อนุมัติก่อนเริ่ม
3. ทำเสร็จ + ทดสอบผ่าน + commit/push แล้ว ให้ติ๊ก `- [x]` พร้อมวันที่ในไฟล์นี้ทันที (เป็นส่วนหนึ่งของ commit เดียวกัน)
